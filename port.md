```
1. SMTP / SMTP STARTTLS - TCP 25 / SMTP & SMTP STARTTLS共用一個連接埠。

2. SMTP Submission - TCP 587 / SMTP寄信協定連接埠25之外的另一個選擇 (STARTTLS)

3. SMTP SSL - TCP 465 / SMTP寄信協定加密模式使用此連接埠 (SSL)

4. POP3 - TCP 110 / POP3收信協定使用此連接埠 (未加密)

5. POP3 SSL - TCP 995 / POP3收信協定加密模式使用此連接埠 (SSL)

6. IMAP v4rev1 - TCP 143 / IMAP4收信協定使用此連接埠 (未加密或加密)

7. IMAP v4rev1 SSL - TCP 993 / IMAP4收信協定加密模式使用此連接埠 (SSL)

8. HTTP (WebMail) - TCP 80 / WebMail使用此連接埠

9. HTTP SSL(WebMail ) - TCP 443 / WebMail加密模式使用此連接埠 (TLS)
```
